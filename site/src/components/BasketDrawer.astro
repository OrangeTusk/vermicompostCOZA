<div class="basket-toast" data-basket-toast role="status" aria-live="polite" hidden><span>Added to your enquiry.</span><a href="/enquiry">Review enquiry</a></div>
<style>
  .basket-toast { position: fixed; z-index: 120; right: 22px; bottom: 22px; display: flex; align-items: center; gap: 18px; padding: 14px 16px; background: var(--ink); color: var(--white); box-shadow: var(--shadow); font-size: .8rem; }.basket-toast a { color: var(--white); font-weight: 600; text-underline-offset: 4px; }
  @media (max-width: 560px) { .basket-toast { left: 14px; right: 14px; justify-content: space-between; } }
</style>
